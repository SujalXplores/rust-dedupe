[package]
name = "dedupe"
version = "1.0.0"
edition = "2021"
description = "A fast, std-only recursive duplicate-file finder."
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = true
